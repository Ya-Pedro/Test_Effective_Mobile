version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: py.Dockerfile
    container_name: backend
    expose:
      - "8080"

  nginx:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    container_name: nginx
    ports:
      - "80:80"